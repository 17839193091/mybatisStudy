<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.chapter4.mapper.StudentHealthFemaleMapper">

    <select id="findStudentHealthFemaleByStuId" parameterType="int" resultType="com.learn.chapter4.po.StudentHealthFemaleBean">
        select * from t_student_health_female where student_id=#{studentId}
    </select>

</mapper>