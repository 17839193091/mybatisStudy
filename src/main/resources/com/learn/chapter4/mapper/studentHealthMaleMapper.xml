<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.chapter4.mapper.StudentHealthMaleMapper">

    <select id="findStudentHealthMaleByStuId" parameterType="int" resultType="com.learn.chapter4.po.StudentHealthMaleBean">
        select * from t_student_health_male where student_id=#{studentId}
    </select>

</mapper>